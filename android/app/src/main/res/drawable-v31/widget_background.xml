<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) widget background. Uses the system
    widget-corner-radius token so the widget's shape matches
    whatever shape the launcher draws its own UI chrome with
    (Pixel Launcher: 28dp, Samsung One UI: 20dp, etc).
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@android:dimen/system_app_widget_background_radius" />
    <solid android:color="@color/widget_background" />
</shape>
