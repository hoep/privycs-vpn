# Gradle
.gradle/
build/
local.properties
*.iml
.idea/

# Kotlin
captures/

# NDK / strongSwan prerequisite build outputs
.build-cache/

# strongSwan submodule-local build byproducts (safety net in case the
# submodule's own .gitignore drifts; these are regenerated by the prep
# script).
vendor/strongswan/**/config.log
vendor/strongswan/**/config.status
