v1.1.5.5 (build 101) — widget protocol switch, in-place + reload/retry.

Root cause found: the app's connect path starts the tunnel with a reload+retry
loop, because right after saving the config the system briefly reports it
stale/invalid (surfaces as "configuration type is wrong"). The widget started
ONCE → failed. The widget now mirrors the app exactly: deactivate other VPNs
(single active), save, then start with up to 8 reload+retry attempts. Switch
happens IN PLACE again (no app launch). build 100's app-handoff is reverted.

HOW TO TEST
- Connection with WG + IPSec (e.g. "Privycs Shielded"): from the widget tap the
  WireGuard pill → it switches to WireGuard in place; tap IPSec → back. The
  active protocol on the widget + Settings ▸ VPN should match.
- If it still fails: open the app → Settings → Logs and send the
  "widget switch[" lines (now show each start attempt + reload).

REGRESSION CHECK
- Widget connect/disconnect disc; IPSec VPN-IP (private only); OpenVPN counter;
  WG/AWG; in-app protocol pills.
