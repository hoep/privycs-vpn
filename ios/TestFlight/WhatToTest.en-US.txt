v1.0.8 — the 1.0.8 release. A full pass is appreciated:

1. On-Demand & Network Rules: set rules (e.g. "this Wi-Fi → no VPN" above
   "any → connect"); the top rule must win, edits apply immediately, and it must
   follow the rules in the background. Allow the Location prompt (needed for
   Wi-Fi-name rules). No state flapping. Pause lets traffic flow.
2. iPad: the sidebar + detail layout — all screens, rotation, split view.
3. Connect screen: protocol-name pills (only the active one coloured), endpoint
   host shown.
4. Help: the in-app guide opens; links in the text open the right pages.
5. Connect/disconnect each protocol; no crashes.

Thanks for testing the 1.0.8 candidate!
