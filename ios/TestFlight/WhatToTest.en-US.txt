v1.1.5.23 (build 119) — tvOS: Settings screen, top bar, button/colour fixes.

- NEW Settings screen (gear, top-right): GLOBAL DNS override (comma-separated,
  applied to EVERY protocol — WG/AWG/OpenVPN), Kill Switch, anonymous crash
  reports, gateway shown, Unlink this TV, and version. (No on-demand/network-rules/
  file-import — not applicable on a TV.)
- "Unlink this TV" MOVED into Settings → off the main screen (less scrolling) and
  no more red-on-red unreadable button; refresh stays teal + readable.
- Top bar with the Privycs logo + a Settings gear; tighter spacing.

DNS override is GLOBAL across all protocols (not per-protocol). If you meant
protocol failover (route via another protocol on failure), that's a separate add.

HOW TO TEST (Apple TV)
- Open Settings (gear) → set a DNS, toggle Kill Switch, both persist + apply on the
  next connect for any protocol. Unlink lives here now.
- Main screen needs less scrolling. Send a connected-screen screenshot.
