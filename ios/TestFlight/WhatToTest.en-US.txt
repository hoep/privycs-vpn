v1.1.5.22 (build 118) — tvOS polish toward iPad parity.

- Connect control is now the big circular DISC with the active protocol's brand
  LOGO in the centre (WireGuard/AmneziaWG/OpenVPN), like the phone app.
- Traffic: BAR-chart sparkline (not lines) in iOS colors — Download green (#22C55E),
  Upload blue (#3B82F6) — with total + live speed.
- Added LAST HANDSHAKE row + a TUNNEL HEALTH pill (Healthy/Degraded from handshake
  age) when connected.
- Server cards now ALWAYS show the protocol logo + a clear selected state (teal
  border + filled radio) so picking among multiple configs is obvious.
- Buttons: refresh = teal, Unlink = red — readable in BOTH light and dark (the app
  follows the TV's appearance now).

No on-demand rules / no autostart on tvOS (by design — not shown).

HOW TO TEST (Apple TV)
- Disc shows the protocol logo; Connect → bars move (green/blue), handshake +
  health appear. Multiple servers: focus a card + press select → teal border marks
  it; Connect uses it. Toggle TV Light/Dark → colors adapt.
- Send a screenshot of the connected screen for final spacing tweaks.
