v1.1.5.41 (build 137) — tvOS: Privycs logo + AmneziaWG logo now actually render.

Root cause of the missing logos: the imagesets were declared "scale: 3x" (copied
from iOS), and tvOS doesn't use 3x → the asset resolved to nothing (blank). Fixed:
- Privycs logo: now the design's 256px logo, universal scale → shows in the rail.
- AmneziaWG: was a 3x-only PNG (blank on tvOS) → universal now → the real logo shows
  on the dial + config cards.
- WireGuard/OpenVPN/IPSec were SVG (fine) — restored their catalog entries.

HOW TO TEST (Apple TV): the Privycs logo shows top-left in the rail; the dial +
config rows show the correct protocol logos (incl. the real AmneziaWG). Connect
button (below the dial) is reachable. Screenshot welcome for final spacing.
