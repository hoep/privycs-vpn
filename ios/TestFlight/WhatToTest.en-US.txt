v1.1.5.13 (build 109) — same as 108 (widget traffic counter reset-on-switch +
periodic refresh) plus a tvOS build fix (WidgetKit isn't available to the tvOS
tunnel target — guarded with #if os(iOS)). iOS behaviour identical to build 108.

HOW TO TEST (widget traffic counters)
- Switch protocols from the widget → counter resets to 0, then climbs as traffic
  flows (updates every ~30s+; iOS throttles widget refreshes — not per-second).
- Leave connected a few minutes → rx/tx advances. For live per-second numbers,
  open the app.

REGRESSION: switch sync (build 107) + all-protocol switch (build 106) intact.
