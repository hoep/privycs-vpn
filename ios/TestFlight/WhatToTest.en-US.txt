v1.1.5.64 (build 160) — tvOS: import address shown larger + as http://ip:port.

The address under "or open the address" now shows the short base URL with its scheme
— http://<ip>:<port> — bigger and readable (the page is served at the root with the
PIN pre-filled, so the long /link?pin path isn't needed when typing it manually). It's
plain HTTP (local network only, PIN-gated); the QR still carries the full URL.

HOW TO TEST: VPN → Add / restore → step 1 shows e.g. http://192.168.x.x:8770, large
and legible; type it in a phone browser on the same Wi-Fi → the upload page opens.
