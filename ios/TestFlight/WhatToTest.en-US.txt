v1.1.5.2 (build 98) — widget-switch diagnostics + IPSec VPN-IP fix.

1) WIDGET PROTOCOL PILLS still don't switch after the entitlement change, so this
   build adds a diagnostic trace to pinpoint why. PLEASE: tap a protocol pill in
   the widget, then open the app → Settings → Logs and look for lines starting
   with "widget switch[" and send them to me. (They log identifiers only, never
   secrets.) That tells me exactly where it fails (keychain miss/error, no target,
   manager, start) so I can fix it precisely.
2) IPSEC VPN IP: the Connect screen no longer shows public (global) IPv6
   addresses as the VPN IP — only the assigned private VPN address (IPv4 +, if
   present, a private ULA IPv6). Verify IPSec shows just the private VPN IP now.

HOW TO TEST
- Widget: tap a protocol pill → (note whether it switches) → send the
  "widget switch[...]" log lines.
- IPSec: connect → Connect screen "VPN IP" shows only the private address, no
  public v6.

REGRESSION CHECK
- OpenVPN + WG/AWG counters & info; IPSec counter; double-tap guard; pools.
