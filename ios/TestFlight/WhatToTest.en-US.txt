v1.1.5.62 (build 158) — tvOS: fix top buttons clipped by the header.

The "Add / restore" and "Refresh" buttons at the top of the VPN tab were slightly cut
off at the top — their focus lift was clipped by the ScrollView edge under the screen
header. Added top padding inside the scroll content so focused buttons have room.

HOW TO TEST: VPN tab → focus the Add/restore + Refresh buttons → the top edge should
no longer be clipped.
