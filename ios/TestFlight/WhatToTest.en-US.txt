v1.1.5.14 (build 110) — widget traffic counter now advances live (projected).

The real fix: the tunnel now publishes live throughput (bytes/sec), and the widget
emits a timeline that PROJECTS the byte counters forward at that speed — so the
counter visibly climbs every ~10s for minutes WITHOUT needing an iOS refresh (iOS
throttles widget refreshes to minutes when the app is closed; it previously looked
frozen). Each real refresh (~every 60s when iOS allows) resyncs to the true value.

HOW TO TEST
- Connect + generate traffic (load a page / speed test). Watch the WIDGET counter:
  rx/tx should now climb continuously (every ~10s), not sit frozen.
- Switch protocols → counter resets to 0 then climbs on the new protocol.
- Idle (no traffic) → counter holds steady (correct).
- Numbers are a projection between refreshes; the app shows exact per-second.

REGRESSION: in-app counter exact; switch + sync (builds 106/107) intact; tvOS.
