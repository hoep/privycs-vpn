v1.1.5.25 (build 121) — tvOS: fix focus traversal + white button text.

- Marked the top bar and the server row as focus sections (.focusSection()) so the
  remote moves cleanly between top bar ⇄ connect disc ⇄ server cards — fixes focus
  getting stuck (the nested scroll was trapping it).
- Refresh moved up next to Settings in the top bar (keeps the server row a clean
  focus target).
- Both top-bar buttons are now borderedProminent: teal fill with WHITE text, so
  they read in BOTH light and dark (no more dark text on the green focused button).

HOW TO TEST (Apple TV)
- Navigate everywhere with the remote — top bar (Refresh/Settings), the disc, and
  the server cards should all be reachable, nothing traps. In LIGHT mode the
  Refresh/Settings text is white on teal (readable).
