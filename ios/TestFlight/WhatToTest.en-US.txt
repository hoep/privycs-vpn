v1.1.5.17 (build 113) — tvOS app: real connect screen with status + traffic.

Apple TV redesign (iPhone/iPad app as the model):
- Big connection-status hero (clear Connected/Disconnected, protocol, uptime).
- Live TRAFFIC counters: Download/Upload cards with total + live speed + sparkline
  (the data was already there — now shown; speed derived like the phone app).
- Connection details: endpoint + VPN IP.
- Server list + connect/disconnect unchanged (works); Unlink kept.

NOTE — "Link a TV" (device-code enrollment) needs the GATEWAY endpoint
(/api/v1/tv/device/start) deployed to production; it currently returns 405 (not
live). Until the gateway ships it, use the manual fallback on the enroll screen
(gateway URL + token). That's a server deploy, not a client change.

HOW TO TEST (Apple TV)
- Enroll (manual fallback for now) → pick a server → Connect → the status hero
  goes green, traffic counters climb, endpoint/VPN IP show. Disconnect works.

(iOS app rebuilt unchanged — shared version file triggers both.)
