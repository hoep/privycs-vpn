v1.1.5.54 (build 150) — tvOS: fix pool-config dialog clipping.

The pool-config screen (gear) was cut off at the bottom and the "Activate pool"
button was clipped / flush-left. Fixed: added side + bottom breathing room inside the
scroll area so focused buttons aren't clipped by the scroll bounds (tvOS focus lift),
widened the side margins, and made "Activate pool" a full-width button.

HOW TO TEST: Configs → gear on a pool → scroll to the bottom; the "Activate pool"
button should be fully visible and centered, nothing cut off at the edges.
