v1.0.8.1 (build 55) — small Connect-screen fix:

The protocol pills under the Connect button (now showing the full protocol
names) used to wrap onto two lines when a connection had several protocols.
They are now a single horizontal row you swipe left/right — matching the
Android app. Please confirm: one line, swipeable, only the active protocol is
brand-coloured, and tapping a pill still opens the config picker.
