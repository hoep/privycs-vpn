v1.1.5.38 (build 134) — tvOS visual polish (Apple TV HIG): depth, materials, glow.

A pass to make the app look professional instead of flat (per Apple's tvOS HIG —
depth/materials/glow at 10-foot distance, not flat fills):
- Background: dark→teal gradient with a soft teal glow (was a flat colour).
- Status hero: larger SF-Display headline with a teal glow when connected.
- Connect disc: outer glow ring + soft shadow.
- Cards (traffic / details / servers / enroll): frosted .ultraThinMaterial with
  larger radius + depth shadow, so they float on the gradient.

HOW TO TEST (Apple TV)
- The connect + enroll screens should look noticeably more "premium" (glow,
  frosted cards, depth). Function unchanged (connect/Always-on/SSID still work).
- Send a screenshot of the connected screen so I can fine-tune spacing/sizes.
