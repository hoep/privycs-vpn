On-Demand & Network Rules — please check:

1. LOCATION: allow the Location prompt (iOS only reveals the Wi-Fi name with
   location granted; without it SSID rules can't match). If you have an
   SSID/BSSID rule but deny location, the screen now shows a clear warning with
   an "Open Settings" button — verify it appears and disappears once granted.

2. RULE ORDER (priority, top wins): e.g. top "Wi-Fi SSID = <yours> -> Disconnect",
   bottom "Any -> Connect". On that Wi-Fi the VPN must stay OFF; elsewhere ON.

3. LIVE EVALUATION card now shows the actual decision, e.g. "Rule matches —
   engine acts: Disconnect" / "...: Connect". Confirm it reflects reality.

4. The whole On-Demand & Network Rules screen is now fully translated — switch
   the app language (Settings > Language) and check the rules/eval text.

5. No idle flapping of the network state; Pause lets traffic flow.

Thanks!
