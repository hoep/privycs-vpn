v1.1.5.40 (build 136) — tvOS: Connect reachable again + dial matches the design.

- FIX: I'd made the dial itself the button — a custom control with blur/shadow
  won't take focus on tvOS, so Connect was unreachable. The dial is now display-
  only and there's a real, reachable Connect/Disconnect button right below it
  (simple .card button, like the rail items that DO focus).
- The dial now matches the mockup: bright teal radial gradient (#1ED4B0→#0BA98B→
  #0C8E76) with a white circle holding the REAL protocol logo (teal-tinted), grey
  when disconnected.

HOW TO TEST (Apple TV): focus the "Connect" button below the dial → press → it
connects. Then send a screenshot of the Connect screen + rail so I can match the
remaining spacing/sizes to the mockup exactly.
