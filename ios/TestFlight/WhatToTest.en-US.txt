v1.1.5.16 (build 112) — widget traffic: REAL values + real graph (no fake projection).

build 111 projected the counter at the last speed, which drifted from actual
traffic ("counts something, not the traffic"), and the graph never updated. This
build:
- Removes the projection — the widget shows the tunnel's REAL byte counts + speed.
- The tunnel now also publishes a rolling throughput history, so the sparkline
  shows REAL recent traffic (independent of the app being open).

HONEST LIMIT: iOS refreshes a home-screen widget only every few minutes when the
app is closed (WidgetKit budget — not changeable). So the counter + graph are
accurate but step periodically, NOT per-second. For live per-second numbers, open
the app.

HOW TO TEST
- Connect + traffic, leave the widget a few minutes → rx/tx + graph update to real
  values on each refresh (not live, but correct).
- Switch protocol → resets to 0.

REGRESSION: in-app exact live counter; switch + sync intact; tvOS.
