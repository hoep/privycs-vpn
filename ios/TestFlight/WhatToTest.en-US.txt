On-Demand rule edits — please verify they take effect immediately:

1. While CONNECTED on a Wi-Fi that has a "this Wi-Fi → no VPN" rule: toggle that
   rule OFF then ON again. The VPN must now DISCONNECT right away (before, the
   live-evaluation said "Disconnect" but the active connection stayed up).
2. Delete or reorder a rule — the change should apply to the current network
   immediately, not only after the next network change.
3. Re-enabling a connect rule on its network should bring the VPN up.

Also still confirming from recent builds: top rule wins (priority), Location
prompt for Wi-Fi-name rules, native iPad sidebar layout, active-only coloured
protocol pill. Thanks!
